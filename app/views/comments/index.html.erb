<div class="navbar">
  <%= link_to 'Back', @photo %>
  <h1>Comments</h1>
</div>

<p id="notice"><%= notice %></p>

<section>
  <%= render @comments, photo: @photo %>
</section>

<%= render 'form', comment: @new_comment %>
